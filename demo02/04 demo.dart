/**
 
Dart的命名规则：

    1.变量名称必须由数字、字母、下划线和美元符（$）组成。

    2.注意：标识符开头不能是数字

    3.标识符不能是保留字和关键字。

    4.变量的名字是区别大小写的，如：age和Age是不同的变量。在实际的运用中，建议，不要用一个单词命名

    5.标识符（变量名称）一定要见名思意：变量名称建议用名词，方法名称建议用动词
 */

void main(){

  // var str1="111";

  // var 2str="XX"; //错误

  // var if="222"; //错误

// //变量区分大小写
//   var age=20;
//   var Age=30;
//   print(age);
//   print(Age);

}